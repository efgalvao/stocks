<%= form_with(model: dividend, local: true) do |form| %>
  <% if dividend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dividend.errors.count, "error") %> prohibited this dividend from being saved:</h2>

      <ul>
        <% dividend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :facade_id %>
    <%= form.text_field :facade_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
