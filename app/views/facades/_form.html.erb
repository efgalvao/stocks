<%= form_with(model: facade, local: true) do |form| %>
  <% if facade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facade.errors.count, "error") %> prohibited this facade from being saved:</h2>

      <ul>
        <% facade.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
